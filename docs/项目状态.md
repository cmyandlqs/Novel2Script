# 项目状态

## 当前阶段

- 当前阶段：阶段 3：章节解析与输入校验
- 当前状态：待开始
- 上一阶段：阶段 2：工程骨架与基础运行，已完成
- 下一步：创建 `feat/chapter-parser` 分支，实现正式章节解析和 3+ 章节校验
- 状态更新时间：2026-06-05

## 阶段总览

| 阶段 | 状态 | PR | 关键产物 |
| --- | --- | --- | --- |
| 阶段 0：竞赛与工程约束固化 | 已完成 | - | `CLAUDE.md`、`AGENTS.md`、`docs/竞赛文档.md`、`docs/开发计划.md`、`docs/技术选型.md` |
| 阶段 1：YAML Schema 与输出契约 | 已完成 | #1 | `schemas/script.schema.json`、`docs/YAML_Schema设计说明.md`、`examples/sample-novel.md`、`examples/output-script.yaml` |
| 阶段 2：工程骨架与基础运行 | 已完成 | #3 | `package.json`、`package-lock.json`、`src/`、README、基础工作台页面 |
| 阶段 3：章节解析与输入校验 | 待开始 | - | 待实现章节解析和 3+ 章节校验 |
| 阶段 4：多阶段 Agent Pipeline | 未开始 | - | 待实现 mock provider 和真实 LLM provider |
| 阶段 5：YAML 校验与质量评分 | 未开始 | - | 待实现 YAML parse、Schema 校验和评分面板 |
| 阶段 6：可编辑剧本工作台 | 未开始 | - | 待实现结构化编辑、YAML 预览和导出 |
| 阶段 7：示例数据与可复现 Demo | 未开始 | - | 待完善示例和 Demo 脚本 |
| 阶段 8：README、视频和最终提交 | 未开始 | - | 待补充 README、Demo 视频链接和最终公开检查 |

## 最近完成阶段验收项

阶段 2：工程骨架与基础运行需要完成：

- [x] 初始化 Next.js + TypeScript + Tailwind 工程骨架。
- [x] 添加正式 `package.json`、lockfile 和基础 npm scripts。
- [x] 添加 `.gitignore`，避免提交 `.env`、构建产物和依赖目录。
- [x] 添加基础 README，说明项目用途、运行方式、依赖和当前 Demo 路径。
- [x] 创建主工作台页面，第一屏展示实际工具而不是营销页。
- [x] 页面包含小说输入区、章节解析占位区、生成按钮、YAML 输出区和校验状态区。
- [x] 使用 mock YAML 输出打通基础页面，不依赖真实 LLM。
- [x] 本地开发服务器能启动，主分支保持可运行。

## 当前验证结果

阶段 2 PR 已完成以下验证：

- `npm run typecheck`：通过。
- `npm run lint`：通过。
- `npm test`：通过，当前阶段尚无测试文件。
- `npm run build`：通过。
- `npm audit --audit-level=moderate`：通过，0 个漏洞。
- `curl -I http://127.0.0.1:3000`：返回 HTTP 200。
- `curl http://127.0.0.1:3000/api/mock`：返回原创小说样例和 YAML 输出样例。

## 当前阶段验收项

阶段 3：章节解析与输入校验需要完成：

- [ ] 将阶段 2 的章节预览逻辑沉淀为正式章节解析模块。
- [ ] 支持常见章节格式：`第1章`、`第一章`、`Chapter 1`、Markdown 标题。
- [ ] 明确校验输入必须包含 3 个以上章节。
- [ ] 少于 3 章时给出清晰错误。
- [ ] 页面展示章节标题、顺序、字数和校验状态。
- [ ] 添加章节解析单元测试。
- [ ] 更新 README 或相关文档说明当前输入规则。

## 当前仓库状态

- GitHub 仓库：https://github.com/cmyandlqs/Noverl2Script
- 当前可见性：`PRIVATE`
- 主分支：`main`
- 当前要求：开发阶段保持 private，等竞赛规定时间再公开。

## 状态维护规则

### 阶段开始

- 更新本文档的“当前阶段”和“当前阶段验收项”。
- 如阶段变化会影响 agent 入口，同时更新 `CLAUDE.md` 和 `AGENTS.md` 的“当前开发状态”。
- 通过单独 PR 合并状态更新。

### 阶段内普通 PR

- PR 描述必须说明属于哪个阶段、做了什么、如何验证。
- 如只是阶段内小任务，可以只更新本文档中的验收项。
- 不必每个小 PR 都更新根目录 agent 文档。

### 阶段完成

- 更新本文档的阶段总览、当前阶段、验收项和下一步。
- 更新 `docs/开发计划.md` 的“当前优先级”。
- 更新 `CLAUDE.md` 和 `AGENTS.md` 的“当前开发状态”。
- 确认 `CLAUDE.md` 与 `AGENTS.md` 内容完全一致。
- 通过 PR 合并到 `main`。
