# 项目状态

## 当前阶段

- 当前阶段：阶段 8：README、视频和最终提交
- 当前状态：最终提交准备中，Demo 体验优化已完成（PR #15）
- 上一阶段：阶段 7：示例数据与可复现 Demo，已完成（PR #12）
- 下一步：补充 Demo 视频链接，最终检查 README，等竞赛规定时间后公开仓库
- 状态更新时间：2026-06-07

## 阶段总览

| 阶段 | 状态 | PR | 关键产物 |
| --- | --- | --- | --- |
| 阶段 0：竞赛与工程约束固化 | 已完成 | - | `CLAUDE.md`、`AGENTS.md`、`docs/竞赛文档.md`、`docs/开发计划.md`、`docs/技术选型.md` |
| 阶段 1：YAML Schema 与输出契约 | 已完成 | #1 | `schemas/script.schema.json`、`docs/YAML_Schema设计说明.md`、`examples/sample-novel.md`、`examples/output-script.yaml` |
| 阶段 2：工程骨架与基础运行 | 已完成 | #3 | `package.json`、`package-lock.json`、`src/`、README、基础工作台页面 |
| 阶段 3：章节解析与输入校验 | 已完成 | #4 | `src/lib/chapters/parseChapters.ts`、章节解析测试、README 输入规则 |
| 阶段 4：多阶段 Agent Pipeline | 已完成 | #5, #6 | `src/lib/ai/`（types/provider/mock/openai/pipeline）、`/api/generate`、中间产物展示 |
| 阶段 5：YAML 校验与质量评分 | 已完成 | #8 | `src/lib/validation/`（validateDraft/scoreDraft）、`/api/validate`、校验结果和评分面板 |
| 阶段 6：可编辑剧本工作台 | 已完成 | #10 | 可编辑人物/场景/beat、YAML 实时同步、重新校验和导出 |
| 阶段 7：示例数据与可复现 Demo | 已完成 | #12 | `docs/Demo操作指南.md`、Demo 自动化测试（示例 ≥ 85 分） |
| 阶段 8：README、视频和最终提交 | 进行中 | #15 | 流式 Demo 反馈、输入策略优化、文本上传、成熟剧本格式调研；待补充 Demo 视频链接和最终公开检查 |

## 最近完成阶段验收项

阶段 8 内 Demo 体验优化（PR #15）：

- [x] `/api/generate/stream` 提供真实流式 Pipeline 步骤反馈。
- [x] 前端展示 6 个生成步骤的等待、进行中、完成和错误状态。
- [x] Schema 允许 1 章及以上结构化生成，3+ 章节要求转移到竞赛 Demo 合规评分。
- [x] 支持 `.txt`、`.md`、`.markdown` 文本上传。
- [x] 人工编辑后提示 YAML 已同步，并引导重新校验后导出。
- [x] Mock Provider 覆盖所有输入章节，不再只处理前 3 章。
- [x] 补充成熟剧本格式调研文档。
- [x] 自动化验证通过：`npm test`、`npm run typecheck`、`npm run lint`、`npm run build`。

## 当前阶段验收项

阶段 8：README、视频和最终提交需要完成：

- [ ] README 包含项目简介、赛题目标、运行方式、依赖说明、原创功能、YAML Schema 入口、示例输入输出。
- [ ] Demo 视频可访问、可播放，有声音讲解，展示主要功能和效果。
- [ ] README 包含 Demo 视频链接。
- [x] 主分支能启动并复现 Demo。
- [ ] 仓库在竞赛规定时间公开。
- [ ] 确认无密钥、无 .env、无侵权素材。

## 当前仓库状态

- GitHub 仓库：https://github.com/cmyandlqs/Noverl2Script
- 当前可见性：`PRIVATE`
- 主分支：`main`
- 当前要求：开发阶段保持 private，等竞赛规定时间再公开。

## 状态维护规则

### 阶段开始

- 更新本文档的"当前阶段"和"当前阶段验收项"。
- 如阶段变化会影响 agent 入口，同时更新 `CLAUDE.md` 和 `AGENTS.md` 的"当前开发状态"。
- 通过单独 PR 合并状态更新。

### 阶段内普通 PR

- PR 描述必须说明属于哪个阶段、做了什么、如何验证。
- 如只是阶段内小任务，可以只更新本文档中的验收项。
- 不必每个小 PR 都更新根目录 agent 文档。

### 阶段完成

- 更新本文档的阶段总览、当前阶段、验收项和下一步。
- 更新 `docs/开发计划.md` 的"当前优先级"。
- 更新 `CLAUDE.md` 和 `AGENTS.md` 的"当前开发状态"。
- 确认 `CLAUDE.md` 与 `AGENTS.md` 内容完全一致。
- 通过 PR 合并到 `main`。
