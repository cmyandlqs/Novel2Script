# 项目状态

## 当前阶段

- 当前阶段：阶段 5：YAML 校验与质量评分
- 当前状态：待开始
- 上一阶段：阶段 4：多阶段 Agent Pipeline，已完成（PR #5, #6）
- 下一步：实现 YAML parse 校验、Schema 校验和质量评分面板
- 状态更新时间：2026-06-06

## 阶段总览

| 阶段 | 状态 | PR | 关键产物 |
| --- | --- | --- | --- |
| 阶段 0：竞赛与工程约束固化 | 已完成 | - | `CLAUDE.md`、`AGENTS.md`、`docs/竞赛文档.md`、`docs/开发计划.md`、`docs/技术选型.md` |
| 阶段 1：YAML Schema 与输出契约 | 已完成 | #1 | `schemas/script.schema.json`、`docs/YAML_Schema设计说明.md`、`examples/sample-novel.md`、`examples/output-script.yaml` |
| 阶段 2：工程骨架与基础运行 | 已完成 | #3 | `package.json`、`package-lock.json`、`src/`、README、基础工作台页面 |
| 阶段 3：章节解析与输入校验 | 已完成 | #4 | `src/lib/chapters/parseChapters.ts`、章节解析测试、README 输入规则 |
| 阶段 4：多阶段 Agent Pipeline | 已完成 | #5, #6 | `src/lib/ai/`（types/provider/mock/openai/pipeline）、`/api/generate`、中间产物展示 |
| 阶段 5：YAML 校验与质量评分 | 未开始 | - | 待实现 YAML parse、Schema 校验和评分面板 |
| 阶段 6：可编辑剧本工作台 | 未开始 | - | 待实现结构化编辑、YAML 预览和导出 |
| 阶段 7：示例数据与可复现 Demo | 未开始 | - | 待完善示例和 Demo 脚本 |
| 阶段 8：README、视频和最终提交 | 未开始 | - | 待补充 README、Demo 视频链接和最终公开检查 |

## 最近完成阶段验收项

阶段 4：多阶段 Agent Pipeline：

- [x] 定义 GenerationProvider 统一接口（章节摘要、人物抽取、地点抽取、剧情梗概、场景拆分、改编说明）。
- [x] 实现 MockProvider，无 API Key 时能跑完整 pipeline。
- [x] 实现 Pipeline 编排器，顺序执行 6 个步骤并返回中间产物。
- [x] 新增 `/api/generate` POST 接口。
- [x] 页面展示 Pipeline 步骤执行状态。
- [x] 页面展示人物表和场景列表等中间产物。
- [x] 使用 `yaml` 包将 Pipeline 结果序列化为 YAML 文本。
- [x] 生成失败时有明确错误提示。
- [x] 实现 OpenAI 兼容 Provider。
- [x] 有 API Key 时能调用真实模型生成结果。
- [x] 新增 `.env.example` 说明环境变量配置。

## 阶段 4 验证结果

- `npm test`：通过，章节解析 6 个 + Pipeline 7 个，共 13 个测试全部通过。
- `npm run typecheck`：通过。
- `npm run lint`：通过，0 errors，0 warnings。
- `npm run build`：通过。
- Mock Provider：无需 API Key，Pipeline 完整可运行。
- OpenAI Provider：配置 `OPENAI_API_KEY` 后自动切换为真实模型。

## 当前阶段验收项

阶段 5：YAML 校验与质量评分需要完成：

- [ ] 实现 YAML parse 校验（检测语法错误）。
- [ ] 实现基于 `schemas/script.schema.json` 的 Schema 校验（使用 AJV）。
- [ ] 实现业务校验：source.chapters ≥ 3、characters 不为空、scenes 不为空。
- [ ] 实现 dialogue speaker 引用校验（speaker_id 必须存在于人物表）。
- [ ] 实现 scene 引用的 chapter_source 校验。
- [ ] 页面展示校验结果（通过/错误路径/具体问题）。
- [ ] 页面展示质量评分面板（合规性、结构完整度、可编辑性等维度）。
- [ ] 更新 README 或相关文档。

## 当前仓库状态

- GitHub 仓库：https://github.com/cmyandlqs/Noverl2Script
- 当前可见性：`PRIVATE`
- 主分支：`main`
- 当前要求：开发阶段保持 private，等竞赛规定时间再公开。

## 状态维护规则

### 阶段开始

- 更新本文档的"当前阶段"和"当前阶段验收项"。
- 如阶段变化会影响 agent 入口，同时更新 `CLAUDE.md` 和 `AGENTS.md` 的"当前开发状态"。
- 通过单独 PR 合并状态更新。

### 阶段内普通 PR

- PR 描述必须说明属于哪个阶段、做了什么、如何验证。
- 如只是阶段内小任务，可以只更新本文档中的验收项。
- 不必每个小 PR 都更新根目录 agent 文档。

### 阶段完成

- 更新本文档的阶段总览、当前阶段、验收项和下一步。
- 更新 `docs/开发计划.md` 的"当前优先级"。
- 更新 `CLAUDE.md` 和 `AGENTS.md` 的"当前开发状态"。
- 确认 `CLAUDE.md` 与 `AGENTS.md` 内容完全一致。
- 通过 PR 合并到 `main`。
